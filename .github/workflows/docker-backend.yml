name: Build and Push Backend Docker Image
on:
  push:
    branches:
      - main
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
  # 1️⃣ Get the code from your repo
    - name: Checkout repository
      uses: actions/checkout@v4
  # 2️⃣ Login to Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ dckr_pat_5wnI--GuB-4Udd_4vxhVyS7hMY8 }}
  # 3️⃣ Build the backend image
    - name: Build Docker image
      run: |
        docker build -t roivishi/ii-backend:latest ./backend
  # 4️⃣ Push to Docker Hub
    - name: Push Docker image
  run: |
    docker push RoiVishi/ii-backend:latest
